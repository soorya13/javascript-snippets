//as excuted in browser prompt
var str='JavaScript';
var stro=new String('JavaScript');
str.toUpperCase();
str.toLowerCase();
str.charAt(3);
str.charCodeAt(3);
str.indexOf('a');
str.lastIndexOf('a');
str.search('a');//this will allow regex
str.slice(1,3);
str.substring(1,3);
str.substr(1,3);
str.split(',');
str.trim();
str.toUpperCase();
str.toLowerCase();